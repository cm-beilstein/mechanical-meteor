---
phase: 03A.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/pages/[slug].astro
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users can see creation time on blog posts"
    - "Time is displayed alongside date on index page"
    - "Time is displayed on individual post pages"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Blog listing with timestamp"
    - path: "src/pages/[slug].astro"
      provides: "Blog post detail with timestamp"
  key_links:
    - from: "index.astro"
      to: "blog entries"
      via: "entry.data.date and entry.data.time"
      pattern: "entry\\.data\\.(date|time)"
---

<objective>
Add time/timestamp display to blog entries on both the homepage listing and individual post pages.

Purpose: Users can see when posts were created, not just the date.
Output: Updated index.astro and [slug].astro with time display.
</objective>

<context>
@src/pages/index.astro
@src/pages/[slug].astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.astro to display time</name>
  <files>src/pages/index.astro</files>
  <action>
Update the homepage listing to display time in addition to date:
- If time field exists in frontmatter, display it in format "HH:MM" or "h:mm a"
- Format: "Apr 10, 2026 at 3:45 PM"
- Show time only if time field exists, otherwise fall back to date-only display
- Keep existing sorting by date, optionally add time to sort for same-day entries
  </action>
  <verify>
npm run build 2>&1 | grep -E "(error|Error)" || echo "Build passed"
  </verify>
  <done>Index page displays "Apr 10, 2026 at 3:45 PM" format for entries with time</done>
</task>

<task type="auto">
  <name>Task 2: Update [slug].astro to display time</name>
  <files>src/pages/[slug].astro</files>
  <action>
Update the individual post page to display time alongside date:
- Same format as index page: "Apr 10, 2026 at 3:45 PM"
- Keep existing date display structure
- Use existing formattedDate variable or create similar for time
  </action>
  <verify>
npm run build 2>&1 | grep -E "(error|Error)" || echo "Build passed"
  </verify>
  <done>Post detail page displays timestamp in same format as index</done>
</task>

</tasks>

<verification>
- npm run build exits 0
- Homepage shows time alongside date for entries
- Individual post pages show timestamp
</verification>

<success_criteria>
- Users can see creation time on blog posts
- Time display consistent across index and post pages
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03A.1-adding-time-timestamp-to-blog-entries-and-docker-setup-with/03A.1-01-SUMMARY.md`
</output>