---
phase: 02-content-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/utils/markdown.ts
  - content/
  - package.json
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-02
  - CONTENT-03
  - HOME-01
must_haves:
  truths:
    - "Markdown files in /content are parsed at build time."
    - "Each markdown file supports frontmatter for metadata (title, date, etc.)."
    - "Homepage displays a list of blog/news entries sourced from markdown."
    - "Adding a new markdown file automatically updates the homepage list."
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Homepage listing entries from markdown"
    - path: "src/utils/markdown.ts"
      provides: "Markdown ingestion logic (if needed)"
    - path: "content/"
      provides: "Markdown content directory with multiple .md files"
  key_links:
    - from: "src/pages/index.astro"
      to: "content/"
      via: "import and parse markdown files"
      pattern: "import.*from.*content"
---

<objective>
Implement the markdown content ingestion pipeline and update the homepage to list all entries from /content. Ensure that adding a new markdown file automatically updates the homepage list on rebuild.

Purpose: Enable content-driven publishing and validate the core content pipeline.
Output: Homepage lists entries from markdown, ingestion logic is in place, and content updates require no code changes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement markdown ingestion logic</name>
  <files>src/utils/markdown.ts, package.json</files>
  <action>Add a utility (or use Astro Content Collections if available) to read all markdown files in /content, parse frontmatter (title, date, tags), and export as an array of entries. Use gray-matter or Astro built-in if possible.</action>
  <verify>
    <automated>node -e "require('./src/utils/markdown').getAllEntries && console.log('OK')"</automated>
  </verify>
  <done>Markdown ingestion logic exports all entries with metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Update homepage to list entries from /content</name>
  <files>src/pages/index.astro</files>
  <action>Update index.astro to import the entries from the ingestion logic and render a list of titles, dates, and links for each markdown file in /content.</action>
  <verify>
    <automated>npm run build && grep -q 'Welcome to the Markdown Blog' dist/index.html</automated>
  </verify>
  <done>Homepage lists all markdown entries with title and date.</done>
</task>

<task type="auto">
  <name>Task 3: Add test markdown file and verify homepage updates</name>
  <files>content/</files>
  <action>Add a second markdown file with different frontmatter. Rebuild the site and verify both entries appear on the homepage without code changes.</action>
  <verify>
    <automated>cp content/example.md content/test.md && sed -i 's/Welcome to the Markdown Blog/Test Entry/' content/test.md && npm run build && grep -q 'Test Entry' dist/index.html</automated>
  </verify>
  <done>Adding a markdown file updates the homepage list after rebuild.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown → Build | Untrusted content parsed at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | content/ | accept | Content is static, no runtime risk |
| T-02-02 | Information Disclosure | content/ | accept | No sensitive data in markdown |
| T-02-03 | Denial of Service | build | accept | Build failures are non-critical |
</threat_model>

<verification>
- Run 'npm run build' and check homepage lists all markdown entries
- Add a new markdown file and confirm homepage updates after rebuild
</verification>

<success_criteria>
- Homepage lists all entries from /content
- Adding a markdown file updates the homepage after rebuild
- No code changes required for new entries
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-ingestion/02-01-SUMMARY.md`
</output>
