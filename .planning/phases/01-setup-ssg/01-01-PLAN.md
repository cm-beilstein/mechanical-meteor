---
phase: 01-setup-ssg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - /content/
  - /src/
  - public/
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-02
  - HOME-01
  - STYLE-01
must_haves:
  truths:
    - "Astro is installed and configured as the static site generator."
    - "Project directory contains /content for markdown files and /src for templates/components."
    - "A sample markdown file with frontmatter exists in /content."
    - "Running 'npm run build' produces a static site scaffold."
    - "Content, templates, and build logic are separated."
  artifacts:
    - path: "package.json"
      provides: "Astro dependency and scripts"
    - path: "astro.config.mjs"
      provides: "Astro configuration"
    - path: "/content/"
      provides: "Markdown content directory"
    - path: "/src/"
      provides: "Templates/components directory"
    - path: "/content/example.md"
      provides: "Sample markdown file with frontmatter"
  key_links:
    - from: "astro.config.mjs"
      to: "/content/"
      via: "Astro content integration"
      pattern: "contentDir"
    - from: "package.json"
      to: "astro"
      via: "dependencies/scripts"
      pattern: "astro build"
---

<objective>
Scaffold the project with Astro as the static site generator, establish the core directory structure, and verify the build process outputs a static site scaffold. This sets the foundation for all content and feature development.

Purpose: Ensure best practices from the start, avoid future migration pain, and enable markdown-driven content ingestion.
Output: Astro project scaffold, /content directory, sample markdown file, working build process.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Scaffold Astro project and configure structure</name>
  <files>package.json, astro.config.mjs, tsconfig.json, /src/, public/</files>
  <action>Install Astro (latest), initialize Astro project if not already present, configure astro.config.mjs for content directory, ensure /src and public/ exist, update package.json scripts for Astro build/dev.</action>
  <verify>
    <automated>npm run build && test -f astro.config.mjs && test -d src && test -d public</automated>
  </verify>
  <done>Astro project is scaffolded, config and scripts are present, build runs successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Set up /content directory and sample markdown file</name>
  <files>/content/</files>
  <action>Create /content directory if missing. Add example.md with YAML frontmatter (title, date, tags) and sample content.</action>
  <verify>
    <automated>test -d content && grep -q 'title:' content/example.md && grep -q 'date:' content/example.md</automated>
  </verify>
  <done>/content directory exists with a valid markdown file containing frontmatter.</done>
</task>

<task type="auto">
  <name>Task 3: Verify build process and directory separation</name>
  <files>package.json, astro.config.mjs, /content/, /src/</files>
  <action>Run 'npm run build' to ensure Astro outputs static site. Confirm /content is not mixed with /src or public. Document any issues in SUMMARY.md.</action>
  <verify>
    <automated>npm run build && test -d dist && test -d content && test -d src</automated>
  </verify>
  <done>Build completes, static output is generated, and directory structure is clean.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Project files | Astro CLI modifies project structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json, astro.config.mjs | mitigate | Use official Astro CLI and npm registry only |
| T-01-02 | Information Disclosure | /content/ | accept | No sensitive data in content directory |
| T-01-03 | Denial of Service | npm run build | accept | Build failures are non-critical at this stage |
</threat_model>

<verification>
- Run 'npm run build' and confirm no errors
- Check that /content, /src, and public/ exist and are not mixed
- Confirm example.md has valid frontmatter
</verification>

<success_criteria>
- Astro project is scaffolded and builds successfully
- /content directory exists with a sample markdown file
- Directory structure is clean and ready for content ingestion
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-ssg/01-01-SUMMARY.md`
</output>
