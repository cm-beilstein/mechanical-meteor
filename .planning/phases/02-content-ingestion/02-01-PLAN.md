---
phase: 02-content-ingestion
plan: 01
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - src/content.config.ts
  - src/content/blog/
  - src/pages/index.astro
  - src/pages/[slug].astro
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-02
  - CONTENT-03
  - HOME-01
must_haves:
  truths:
    - "Markdown files in src/content/blog/ are parsed at build time using Astro 6 loader API."
    - "Each markdown file supports frontmatter for metadata (title, date, tags)."
    - "Homepage displays a list of blog entries sourced from markdown."
    - "Adding a new markdown file automatically updates the homepage list."
    - "Individual blog entry pages render markdown as HTML (not raw text)."
  artifacts:
    - path: "src/content.config.ts"
      provides: "Astro 6 content collection config with glob loader"
    - path: "src/content/blog/"
      provides: "Markdown content directory with multiple .md files"
    - path: "src/pages/index.astro"
      provides: "Homepage listing entries from content collection"
    - path: "src/pages/[slug].astro"
      provides: "Individual entry pages with rendered markdown"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/blog/"
      via: "glob loader pattern"
      pattern: "loader: glob({ base: './src/content/blog'"
    - from: "src/pages/index.astro"
      to: "src/content/blog/"
      via: "getCollection('blog')"
      pattern: "getCollection('blog')"
---

<objective>
Verify and document the markdown content ingestion pipeline using Astro 6 content collections. The homepage should list all entries from src/content/blog/, and individual entry pages should render markdown as HTML.

Purpose: Enable content-driven publishing with automatic homepage updates when new markdown files are added.
Output: Working content pipeline, homepage listing, rendered entry pages.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify Astro 6 content collection configuration</name>
  <files>src/content.config.ts, src/content/blog/</files>
  <action>
    Verify the Astro 6 content collection is properly configured:
    - src/content.config.ts uses glob loader from astro/loaders
    - Schema validates: title (string), date (string|date), tags (optional array)
    - src/content/blog/ contains markdown files with valid frontmatter
    - Run 'npm run build' to confirm all entries are processed
  </action>
  <read_first>
    - src/content.config.ts
    - src/content/blog/*.md
  </read_first>
  <acceptance_criteria>
    - npm run build exits 0
    - All markdown files in src/content/blog/ are processed
    - dist/index.html exists and contains entry links
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify homepage listing works</name>
  <files>src/pages/index.astro</files>
  <action>
    Verify the homepage correctly lists all blog entries:
    - index.astro uses getCollection('blog') to fetch entries
    - Each entry displays title, date, and link to detail page
    - Run 'npm run build' and check dist/index.html for entry list
  </action>
  <read_first>
    - src/pages/index.astro
    - dist/index.html
  </read_first>
  <acceptance_criteria>
    - dist/index.html contains list items with entry titles
    - Each entry has a link to its detail page (e.g., /example)
    - Entry dates are formatted correctly
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify entry pages render HTML (not raw markdown)</name>
  <files>src/pages/[slug].astro</files>
  <action>
    Verify individual entry pages render markdown as HTML:
    - [slug].astro uses entry.render() to get Content component
    - Markdown content renders as HTML elements (p, h1, code, etc.)
    - Run 'npm run build' and check dist/example/index.html for rendered HTML
  </action>
  <read_first>
    - src/pages/[slug].astro
    - dist/example/index.html
  </read_first>
  <acceptance_criteria>
    - dist/example/index.html contains rendered HTML (p tags, not raw markdown)
    - Entry data (title, date) displays correctly
    - "Back to Home" link works
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Verify auto-update on new content</name>
  <files>src/content/blog/, dist/</files>
  <action>
    Verify that adding a new markdown file updates the homepage automatically:
    - Create a temporary test file in src/content/blog/
    - Run 'npm run build'
    - Verify the new entry appears on homepage
    - Clean up test file
  </action>
  <read_first>
    - src/content/blog/test.md
    - dist/index.html
  </read_first>
  <acceptance_criteria>
    - Adding a new .md file causes homepage to include new entry after rebuild
    - No code changes required for new entries
    - Test file created and cleaned up
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown → HTML | Untrusted content parsed at build time |
| Content Collection → Pages | Astro validates frontmatter via Zod schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | src/content/blog/ | accept | Content is static, no runtime risk |
| T-02-02 | Information Disclosure | src/content/blog/ | accept | No sensitive data in markdown |
| T-02-03 | Denial of Service | build | accept | Build failures are non-critical |
| T-02-04 | XSS | rendered HTML | mitigate | Astro sanitizes markdown HTML |
</threat_model>

<verification>
- Run 'npm run build' and confirm all pages generate
- Check homepage lists all entries from src/content/blog/
- Check entry pages render HTML (not raw markdown)
- Verify new markdown files auto-update homepage
</verification>

<success_criteria>
- Homepage lists all entries from src/content/blog/
- Individual entry pages render markdown as HTML
- Adding a markdown file updates homepage after rebuild
- No code changes required for new entries
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-ingestion/02-01-SUMMARY.md`
</output>
