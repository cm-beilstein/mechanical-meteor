---
phase: 01-setup-ssg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/content.config.ts
  - astro.config.mjs
  - tsconfig.json
  - src/content/
  - src/
  - public/
autonomous: true
requirements:
  - SSG selection
  - repo setup
  - initial config
must_haves:
  truths:
    - "Astro 6 is installed and configured as the static site generator."
    - "Project uses Astro 6 loader API (glob loader) for content collections."
    - "Content lives in src/content/ directory with proper collection schema."
    - "Running 'npm run build' produces a working static site with blog entries."
    - "Individual blog entries render as HTML (not raw markdown)."
  artifacts:
    - path: "package.json"
      provides: "Astro dependency and scripts"
    - path: "src/content.config.ts"
      provides: "Astro 6 content collection config with glob loader"
    - path: "src/content/blog/"
      provides: "Markdown content directory with blog collection"
    - path: "src/"
      provides: "Templates/components/layouts"
    - path: "dist/"
      provides: "Static build output"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/blog/"
      via: "glob loader pattern"
      pattern: "loader: glob({ base: './src/content/blog'"
    - from: "package.json"
      to: "astro"
      via: "dependencies/scripts"
      pattern: "astro build"
---

<objective>
Scaffold the project with Astro 6 as the static site generator, establish the Astro 6 loader API content structure, and verify the build process outputs a working static site with rendered markdown. This sets the foundation for all content and feature development.

Purpose: Ensure Astro 6 best practices, proper content collection configuration, and markdown rendering as HTML.
Output: Astro 6 project scaffold, src/content/blog directory, sample markdown files, working build process with rendered content.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify Astro 6 configuration</name>
  <files>package.json, src/content.config.ts, astro.config.mjs</files>
  <action>
    Verify the following are present:
    - package.json contains "astro": "^6.x"
    - src/content.config.ts uses glob loader: `import { glob } from 'astro/loaders'` and `loader: glob({ base: './src/content/blog', pattern: '**/*.{md,mdx}' })`
    - src/content.config.ts schema validates: title (string), date (string|date), tags (optional string array)
  </action>
  <read_first>
    - package.json
    - src/content.config.ts
  </read_first>
  <acceptance_criteria>
    - package.json contains astro version 6.x
    - src/content.config.ts imports glob from astro/loaders
    - src/content.config.ts defines blog collection with loader and schema
    - src/content/blog/ directory exists with markdown files
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify content collection structure</name>
  <files>src/content/blog/</files>
  <action>
    Check that src/content/blog/ contains markdown files with frontmatter:
    - title: string
    - date: string or YAML date format
    - tags: optional array of strings
  </action>
  <read_first>
    - src/content/blog/*.md
  </read_first>
  <acceptance_criteria>
    - src/content/blog/ directory exists
    - At least one .md file with valid frontmatter
    - Frontmatter includes title, date
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Verify build process and HTML rendering</name>
  <files>package.json, src/pages/</files>
  <action>
    Run 'npm run build' and verify:
    - Build completes without errors
    - dist/ contains static HTML files
    - Blog entry pages (e.g., /example/index.html) render markdown as HTML (not raw markdown syntax)
    - Homepage (index.html) lists blog entries
    - Entry pages use the Layout component with rendered Content
  </action>
  <read_first>
    - src/pages/index.astro
    - src/pages/[slug].astro
    - src/layouts/Layout.astro
  </read_first>
  <acceptance_criteria>
    - npm run build exits 0
    - dist/index.html exists
    - dist/example/index.html exists
    - dist/example/index.html contains rendered HTML (p tags, not raw markdown)
    - src/pages/[slug].astro uses entry.render() to render Content component
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Project files | Astro CLI modifies project structure |
| Content files → HTML | Markdown processed by Astro at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json, src/content.config.ts | mitigate | Use official Astro CLI and npm registry only |
| T-01-02 | Information Disclosure | src/content/ | accept | No sensitive data in content directory |
| T-01-03 | Denial of Service | npm run build | accept | Build failures are non-critical at this stage |
</threat_model>

<verification>
- Run 'npm run build' and confirm no errors
- Check that src/content/blog/ contains valid markdown files
- Verify dist/example/index.html renders HTML (not raw markdown)
- Confirm homepage lists blog entries
</verification>

<success_criteria>
- Astro 6 project is scaffolded and builds successfully
- src/content/blog/ directory exists with sample markdown files
- Blog entries render as HTML using Astro 6 loader API and render()
- Directory structure follows Astro 6 best practices
</success_criteria>

<output>
After completion, create `.planning/phases/01-setup-ssg/01-01-SUMMARY.md`
</output>
