---
phase: 03-core-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/[slug].astro
  - src/pages/index.astro
  - src/layouts/Layout.astro
  - src/styles/global.css
  - src/components/
autonomous: true
requirements:
  - HOME-02
  - ENTRY-01
  - ENTRY-02
  - STYLE-01
  - STYLE-02
must_haves:
  truths:
    - "Each entry on the homepage links to its own detail page."
    - "Entry pages display content and metadata (title, date, etc.)."
    - "Entry pages are accessible via unique URLs."
    - "Basic site styling is applied for readability and usability."
    - "Site is responsive and visually coherent across devices."
  artifacts:
    - path: "src/pages/[slug].astro"
      provides: "Dynamic entry detail pages"
    - path: "src/pages/index.astro"
      provides: "Homepage with links to entry pages"
    - path: "src/layouts/Layout.astro"
      provides: "Base layout for styling"
    - path: "src/styles/global.css"
      provides: "Global responsive styles"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/pages/[slug].astro"
      via: "<a href> links with entry slugs"
      pattern: "href={`/${entry.slug}`}"
    - from: "src/pages/[slug].astro"
      to: "content/"
      via: "import.meta.glob for markdown content"
      pattern: "import.meta.glob"
---

<objective>
Implement dynamic routing for entry pages, render entry content and metadata, and apply basic responsive styling to homepage and entry pages.

Purpose: Enable users to view individual entries with full content and metadata, and ensure the site is visually usable on all devices.
Output: Homepage links to entry pages, entry pages display content, and site is styled responsively.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement dynamic routing for entry pages</name>
  <files>src/pages/[slug].astro</files>
  <action>Create [slug].astro to generate a page for each markdown entry using import.meta.glob. Parse slug from URL, load corresponding markdown, and render content and metadata.</action>
  <verify>
    <automated>npm run build && grep -q 'Welcome to the Markdown Blog' dist/test-entry/index.html</automated>
  </verify>
  <done>Each entry has a unique URL and displays its content and metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Update homepage to link to entry pages</name>
  <files>src/pages/index.astro</files>
  <action>Update homepage to wrap each entry title in a link to its detail page using the entry slug.</action>
  <verify>
    <automated>npm run build && grep -q '<a href="/test-entry"' dist/index.html</automated>
  </verify>
  <done>Homepage entries link to their respective detail pages.</done>
</task>

<task type="auto">
  <name>Task 3: Apply basic responsive styling</name>
  <files>src/layouts/Layout.astro, src/styles/global.css</files>
  <action>Add or update CSS for readability (font, spacing, colors) and responsiveness (flex/grid, media queries) in Layout.astro and global.css. Ensure homepage and entry pages look good on mobile and desktop.</action>
  <verify>
    <automated>npm run build && grep -q 'font-family' dist/index.html</automated>
  </verify>
  <done>Site is visually styled and responsive across devices.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown → Page | Untrusted content rendered as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | XSS | [slug].astro | mitigate | Sanitize markdown output or use trusted renderer |
| T-03-02 | Tampering | content/ | accept | Content is static, no runtime risk |
| T-03-03 | Information Disclosure | content/ | accept | No sensitive data in markdown |
</threat_model>

<verification>
- Run 'npm run build' and check homepage links to entry pages
- Visit entry URLs and confirm content and metadata are displayed
- Check site appearance on mobile and desktop
</verification>

<success_criteria>
- Homepage links to entry pages
- Entry pages display content and metadata
- Site is styled and responsive
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-features/03-01-SUMMARY.md`
</output>
