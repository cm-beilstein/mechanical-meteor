---
phase: 03A.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
autonomous: true
requirements: []
must_haves:
  truths:
    - "Application runs in Docker container"
    - "docker-compose up starts the application"
    - "Docker setup is production-ready"
  artifacts:
    - path: "Dockerfile"
      provides: "Container definition for Astro app"
    - path: "docker-compose.yml"
      provides: "Service orchestration"
    - path: ".dockerignore"
      provides: "Build optimization"
  key_links:
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "build context"
      pattern: "build:"
---

<objective>
Containerize the Astro application using Docker and docker-compose.

Purpose: Enable running the application in containers for development and deployment.
Output: Dockerfile, docker-compose.yml, .dockerignore
</objective>

<context>
@package.json (Node version requirement: >=22.12.0)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile</name>
  <files>Dockerfile</files>
  <action>
Create Dockerfile for Astro application:
- Use Node.js 22 LTS image (node:22-bookworm or node:22-alpine)
- Set working directory to /app
- Copy package.json and package-lock.json first (for dependency caching)
- Run npm ci to install dependencies
- Copy remaining source files
- Expose port 4321 (Astro default)
- Use npm run dev for development or npm run preview for production
- Use multi-stage build for production: build stage + runner stage
  </action>
  <verify>
docker build -t astro-blog-test . 2>&1 | tail -20
  </verify>
  <done>Dockerfile created, builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Create docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
Create docker-compose.yml:
- Define 'web' service
- Build from Dockerfile in current directory
- Map port 4321:4321 (host:container)
- Set environment: NODE_ENV=development
- Add volumes for hot reload during development (src/, package.json, astro.config.mjs)
- Set command to npm run dev for live reload
  </action>
  <verify>
docker-compose config
  </verify>
  <done>docker-compose.yml created and validates</done>
</task>

<task type="auto">
  <name>Task 3: Create .dockerignore</name>
  <files>.dockerignore</files>
  <action>
Create .dockerignore to exclude:
- node_modules (will be installed in container)
- .git
- .astro (build cache)
- dist (rebuilt in container)
- .vscode
- README.md
- .planning (GSD planning directory)
- Any other unnecessary files
  </action>
  <verify>
cat .dockerignore
  </verify>
  <done>.dockerignore created with appropriate exclusions</done>
</task>

</tasks>

<verification>
- docker build -t astro-blog-test . succeeds
- docker-compose config validates
- docker-compose up -d starts container successfully
- Application accessible at localhost:4321
</verification>

<success_criteria>
- docker-compose up starts the container
- Application accessible at localhost:4321
- Build completes without errors
- Hot reload works for development
</success_criteria>

<output>
After completion, create `.planning/phases/03A.1-adding-time-timestamp-to-blog-entries-and-docker-setup-with/03A.1-02-SUMMARY.md`
</output>