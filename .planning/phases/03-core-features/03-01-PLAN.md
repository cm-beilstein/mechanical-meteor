---
phase: 03-core-features
plan: 01
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/pages/index.astro
  - src/pages/[slug].astro
  - src/layouts/Layout.astro
  - src/styles/global.css
  - astro.config.mjs
  - package.json
autonomous: true
requirements:
  - HOME-02
  - ENTRY-01
  - ENTRY-02
  - STYLE-01
  - STYLE-02
must_haves:
  truths:
    - "Each entry on the homepage links to its own detail page."
    - "Entry pages display content and metadata (title, date)."
    - "Entry pages are accessible via unique URLs."
    - "Tailwind CSS with custom theme applied for readability."
    - "Site is responsive and visually coherent across devices."
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Homepage with styled entry links"
    - path: "src/pages/[slug].astro"
      provides: "Styled entry detail pages"
    - path: "src/layouts/Layout.astro"
      provides: "Base layout with container and dynamic title"
    - path: "src/styles/global.css"
      provides: "Tailwind CSS with custom theme"
    - path: "astro.config.mjs"
      provides: "Tailwind Vite plugin configuration"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/pages/[slug].astro"
      via: "<a href> links with entry IDs"
      pattern: "href={`/${entry.id}`}"
    - from: "src/layouts/Layout.astro"
      to: "src/styles/global.css"
      via: "import statement"
      pattern: "import '../styles/global.css'"
---

<objective>
Implement dynamic routing for entry pages, render entry content and metadata, and apply Tailwind CSS styling with custom theme for a polished, responsive design.

Purpose: Enable users to view individual entries with full content and metadata, and ensure the site is visually cohesive and responsive.
Output: Homepage links to entry pages, entry pages display styled content, and site is responsive.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Configure Tailwind CSS</name>
  <files>astro.config.mjs, package.json, src/styles/global.css</files>
  <action>
    Install and configure Tailwind CSS v4:
    - Add @tailwindcss/vite plugin to astro.config.mjs
    - Create src/styles/global.css with Tailwind import and custom @theme
    - Define custom colors (primary, background, surface, text)
    - Add component styles (container, article-card, entry-list)
  </action>
  <read_first>
    - astro.config.mjs
    - src/styles/global.css
  </read_first>
  <acceptance_criteria>
    - npm run build exits 0
    - dist/_astro/*.css contains Tailwind output
    - Custom theme variables defined
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update Layout with container and dynamic title</name>
  <files>src/layouts/Layout.astro</files>
  <action>
    Update Layout.astro:
    - Import global.css
    - Add container div wrapper
    - Add dynamic title prop with default
    - Remove inline styles (now handled by Tailwind)
  </action>
  <read_first>
    - src/layouts/Layout.astro
  </read_first>
  <acceptance_criteria>
    - Layout imports global.css
    - Layout has container div with slot
    - Title prop is dynamic
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Style homepage with entry list</name>
  <files>src/pages/index.astro</files>
  <action>
    Update index.astro to use Tailwind classes:
    - Apply entry-list class to ul
    - Apply entry-title and entry-date classes to entry info
    - Add responsive heading styles
  </action>
  <read_first>
    - src/pages/index.astro
    - src/styles/global.css
  </read_first>
  <acceptance_criteria>
    - Homepage uses entry-list class
    - Entry titles and dates are styled
    - dist/index.html contains styled entries
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Style entry pages with article card</name>
  <files>src/pages/[slug].astro</files>
  <action>
    Update [slug].astro to use Tailwind classes:
    - Wrap content in article-card
    - Add back link with styled button
    - Apply typography styles to entry content
  </action>
  <read_first>
    - src/pages/[slug].astro
    - src/styles/global.css
  </read_first>
  <acceptance_criteria>
    - Entry pages use article-card class
    - Back link is styled
    - Content renders with typography styles
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Verify responsive design</name>
  <files>src/styles/global.css</files>
  <action>
    Verify responsive styles:
    - Check media queries for mobile devices
    - Ensure container adapts to screen size
    - Verify card padding adjusts on mobile
  </action>
  <read_first>
    - src/styles/global.css
  </read_first>
  <acceptance_criteria>
    - CSS contains @media queries for max-width: 640px
    - Container padding adjusts on mobile
    - Cards have responsive padding
  </acceptance_criteria>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown → HTML | Untrusted content parsed at build time |
| Tailwind CSS | Client-side styling, no runtime risks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | XSS | [slug].astro | mitigate | Astro sanitizes markdown HTML |
| T-03-02 | Tampering | src/content/ | accept | Content is static, no runtime risk |
| T-03-03 | Information Disclosure | src/content/ | accept | No sensitive data in markdown |
</threat_model>

<verification>
- Run 'npm run build' and verify all pages generate
- Check homepage uses Tailwind classes (entry-list, entry-title)
- Check entry pages use article-card class
- Verify CSS contains responsive styles
- Check Tailwind custom theme variables are defined
</verification>

<success_criteria>
- Homepage links to entry pages with styled cards
- Entry pages display content with article-card styling
- Tailwind CSS with custom theme applied
- Site is responsive with media queries
- Visual design is cohesive across pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-features/03-01-SUMMARY.md`
</output>
